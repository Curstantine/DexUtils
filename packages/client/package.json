{
	"name": "@dexutils/client",
	"version": "0.0.1",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "rimraf dist && yarn run build:types && yarn run build:esm",
		"build:esm": "esbuild src/index.ts --bundle --platform=node --target=es2021 --format=esm --outfile=dist/index.js",
		"build:types": "tsc --emitDeclarationOnly --outDir dist",
		"typecheck": "tsc",
		"lint": "eslint .",
		"lint:fix": "yarn lint --fix",
		"test": "jest",
		"test:watch": "yarn test -- --watch"
	},
	"dependencies": {
		"@dexutils/typings": "workspace:^",
		"form-data": "^4.0.0",
		"node-fetch": "^3.3.0"
	},
	"devDependencies": {
		"@dexutils/eslint-config": "*",
		"@dexutils/jest-config": "*",
		"@types/jest": "^29.2.3",
		"@types/node": "^18.11.9",
		"esbuild": "^0.15.15",
		"esbuild-runner": "^2.2.2",
		"eslint": "^8.28.0",
		"jest": "^29.3.1",
		"rimraf": "^4.1.2",
		"typescript": "^4.9.3"
	}
}
